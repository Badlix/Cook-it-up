{% extends 'base.html.twig' %}

{% block title %}Liste des ingrédients{% endblock %}

{% block body %}
    <div class="flex items-center justify-center min-h-screen bg-gray-100">
        <div class="w-96 max-h-96 bg-white p-6 rounded-lg shadow-lg overflow-auto">
            <h1 class="mb-4 text-2xl font-semibold text-center text-gray-700">Liste des ingrédients</h1>
            <ul class="space-y-2">
                {% for product in products %}
                    <li class="p-2 rounded-md bg-gray-50 shadow text-gray-700 hover:bg-blue-100 hover:text-blue-800 cursor-pointer">
                        {{ product.name }}
                    </li>
                {% else %}
                    <li class="p-2 text-gray-500">Aucun ingrédient disponible.</li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endblock %}